/**
 * Greets someone by name.
 *
 * Deliberately defective: an empty name produces a trailing space and greets
 * nobody. `test/greeting.test.js` states what it should do instead.
 */
export function greeting(name) {
  return `Hello ${name}`;
}
